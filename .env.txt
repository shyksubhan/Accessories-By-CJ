# ─── Acc-by-CJ Frontend Environment Variables ─────────────────────────────────
# Copy this file to: Acc-by-Cj/.env  (root of the project, next to package.json)

# ─── Backend API base URL ─────────────────────────────────────────────────────
# In development this should point to your local backend.
# In production change it to your deployed backend URL.
VITE_API_BASE=http://localhost:3001/api
